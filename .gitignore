node_modules/
.next/
dist/
.site*/
.env
.env.*
!.env.example
# The tracked root and participant-starter .mcp.json hold OAuth-only HTTP
# servers (no tokens). reference-app keeps its own ignore for local configs.
.vercel/
*.log
.DS_Store
Thumbs.db

# Instructor-only sensitive projection assets (wifi/Discord QR) — live on the
# internal Drive and are copied here by start-welcome.cmd at launch; never commit.
materials/instruktor/entry/local/
