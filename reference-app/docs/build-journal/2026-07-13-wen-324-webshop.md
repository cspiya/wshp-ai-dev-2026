# Build journal — WEN-324 webshop purchase journey

**Date:** 2026-07-13

**Issue:** `WEN-324`

**Base:** `main@34ba09f` (accepted Precision Workshop Console UI, WEN-311)

**Branch:** `ai/wen-324-webshop-journey`

**Scope:** the REAL webshop purchase journey per the human-accepted design
mock [`../design/mock-webshop-journey.html`](../design/mock-webshop-journey.html)
(5 screens: catalog, product detail, cart, guest/company checkout with
coupon, confirmation). New `orders` vertical slice + full `/shop` rebuild.
Protected boundaries untouched: Neon Auth semantics, PaymentPort behavior,
existing module contracts (only the sanctioned `root.ts` wiring changed).

This record is written while the package runs. Linear owns work state.

## 1. Acceptance restatement before work

Done means ALL of: a golden-path `orders` module (pure zod domain with the
mock's exact money math, public guest `preview`/`place` procedures, an
`OrderRepo` port with an in-memory adapter passing a shared contract suite,
composition-root wiring + root test); the `/shop` page rebuilt as the mock's
five-view client wizard using the existing console classes with net+VAT+gross
on every price surface; payment via the EXISTING `checkout.authorize` with
the auth id recorded by `orders.place`; a rewritten `e2e/shop.spec.ts`
covering the two-workshop company+coupon guest purchase (`@happy-path`,
`workshops.spec.ts` untouched); all five gates green; evidence screenshots;
DESIGN-GUIDELINE addendum; this journal. No new npm dependencies, English UI,
invented data only. A green build without the journey evidence is not done.

## 2. Money math pinned to the accepted mock

Domain (`src/modules/orders/domain/order.ts`, integer HUF only):

- net subtotal = Σ unitNet × qty; qty 1..5, above 5 the message names the
  custom group offer.
- coupon (invented catalog, `WELCOME10` = 10% off net) **floors**.
- VAT 27% of net-after-discount rounds **half-up** via
  `floor((net × 27 + 50) / 100)` — no floating point.
- Worked numbers reproduced exactly in `order.domain.test.ts`:
  38 500 → −3 850 → 34 650 → VAT 9 356 (9 355.5 half-up) → gross 44 006;
  plus the .5 boundary (50 → 13.5 → 14) and the discount floor (105 → 10).
- Per-seat gross helper `grossFromNet` matches the mock card prices
  (10 000 → 12 700, 18 500 → 23 495, 14 000 → 17 780).

## 3. Decisions (with reasons)

1. **Contact carries `name`** (spec bullet said `{email, phone}`): the
   frontend spec requires name/email/phone as required contact fields and
   the accepted mock's confirmation ledger prints
   `Contact: Kiss Anna · phone · email` — impossible without a stored
   contact name. Deviation said out loud here.
2. **Person-mode buyer name = the contact full name** (mock-faithful): the
   mock's billing module shows extra fields only in company mode; person
   mode shows a mini note ("the buyer name is the full name from the
   contact block") instead of a duplicate input. A `buyer.name` validation
   error is surfaced on the Full name field.
3. **Shop UI lives in `src/modules/orders/ui/`**, not `src/app/shop/`
   (golden-path form, same as `workshops/ui/`). First attempt kept the
   views in `src/app/shop` importing `orders.contract.ts` — that dragged
   the router factory and `@trpc/server` into the client bundle and crashed
   `/shop` at hydration ("You're trying to use @trpc/server in a non-server
   environment"). Client components now import the domain RELATIVELY; the
   server page imports `ShopJourney` through the contract. The contract
   documents the rule.
4. **Unknown coupon is rejected in the router** (`BAD_REQUEST`,
   `Unknown coupon "X" — remove it or use a valid code.`) using the domain's
   `resolveCoupon`/`unknownCouponMessage`, not a zod refine — so the client
   shows a clean human message instead of a serialized issue list.
5. **Client-sent totals are ignored**: `place`'s input schema is a plain
   (non-strict) object, so a smuggled `totals` key is stripped and the
   server recomputes; pinned by a router test.
6. **`checkout.authorize` stays protected and untouched.** Locally the
   in-memory seam supplies the fixed user, so guest checkout works; against
   a deployed preview the e2e spec signs up through the optional account
   panel first (same conditional as the previous spec). PaymentPort
   behavior unchanged; the payment reference is `shop-<timestamp>` because
   the order number does not exist before `place`.
7. **"Download summary" from mock 5/5 was NOT built** — it had no real
   behavior to back it (no PDF/print pipeline), and a dead button would be a
   fabricated affordance.
8. **Billing postal code and city are separate fields** (mock combines them
   in one input) — the domain address has four required parts and separate
   fields validate honestly.
9. **Cart adds navigate to the cart view** (catalog "Add to cart" adds 1
   seat; product page adds the stepper quantity) so the mock's cart-count
   chip is always visibly truthful.

## 4. Drizzle adapter — documented follow-up (not built)

The order aggregate (buyer discriminated union + items array + totals)
needs a jsonb-backed table + migration; that is NOT a quick copy of the
scalar `workshops`/`registrations` schema patterns, so per the spec it is
deferred. Consequences, said honestly:

- `orders` is wired with the in-memory adapter in EVERY environment
  (comment at the composition root, `src/platform/api/root.ts`): orders
  vanish on server restart. Fine for the teaching journey, not production.
- The shared suite `infra/orders.repo.contract.test.ts` is the contract any
  future Drizzle adapter must pass (run under `TEST_DATABASE_URL`, like the
  other modules).
- Follow-up issue to be opened in Linear by the coordinator.

## 5. What was built

- `src/modules/orders/` — golden-path slice: `domain/order.ts` (zod-only),
  `application/orders.router.ts` (+ `OrderRepo` port),
  `application/order.domain.test.ts`, `application/orders.router.test.ts`,
  `infra/in-memory-order-repo.ts`, `infra/orders.repo.contract.test.ts`,
  `ui/` (10 components — journey, five views, price block, qty stepper,
  rail, auth panel), `orders.contract.ts`, `AGENTS.md`, `README.md`,
  `acceptance/orders.feature`.
- `src/platform/api/root.ts` wiring + `root.test.ts` guest-preview
  expectation.
- `src/app/shop/page.tsx` renders `ShopJourney` via the contract;
  `shop-demo.tsx`, old `auth-panel.tsx`/`journey-rail.tsx` moved/removed
  from `src/app/shop/`.
- `src/app/globals.css`: `.price*`, `.qty*`, `.cartline*`, `.sumbox`/
  `.sumrow*`, `.hintbox`, `.okbox`, `.switch-row`, `.rail-5` (documented in
  DESIGN-GUIDELINE §8).
- `src/lib/format.ts`: added `formatHufAmount` (extends, no duplication).
- `e2e/shop.spec.ts` rewritten to the approved journey (see §7);
  `e2e/workshops.spec.ts` untouched.

## 6. Gates (from `reference-app/`, all exit 0)

```
npm run typecheck   → exit 0
npm run lint        → exit 0
npm run test        → exit 0   (94 passed | 11 skipped — the 11 are the
                                Drizzle contract suites, skipped without
                                TEST_DATABASE_URL as designed)
npm run build       → exit 0   (routes: / /shop /workshops + api)
npm run test:e2e    → exit 0   (2 passed: workshops + new shop journey)
```

Worth recording (agent-loop lesson): a PowerShell 5.1
`Get-Content`/`Set-Content` round-trip over the UI files double-encoded
every non-ASCII character (UTF-8 read as cp1252 → mojibake like `Â·`); the
e2e text assertions caught it, and a one-shot reversal script restored the
bytes exactly. Editing files only with the dedicated tools avoids this.

## 7. E2E coverage (the approved feature spec)

`e2e/shop.spec.ts` (`@happy-path`): creates a second workshop through the
existing `/workshops` UI (net 18 500) → catalog → product detail of the
seeded workshop with qty stepped to 2 (keycap names 25,400 HUF gross) →
cart via Add to cart → continue shopping → adds the second workshop →
cart asserts both lines and server totals (38,500 / 10,395 / 48,895) →
guest checkout with company mode (company name + tax number) and full
billing → applies WELCOME10 → asserts discount −3,850, VAT 9,356, gross
44,006 → Continue to payment (authorize + place) → confirmation certificate
asserts order number `REF-2026-\d{4}`, company buyer with tax nr, both
items, coupon, `net 34,650 + VAT 9,356 = gross 44,006 HUF`, `fake_` payment
auth → Back to catalog resets the cart chip to 0. Against a preview the
spec signs up via the optional account panel before paying (protected
`checkout.authorize`).

## 8. Evidence (docs/design/evidence/, dev server E2E_IN_MEMORY_DB=1 :3119)

- `wen324-catalog-desktop.png` (1440) / `wen324-catalog-mobile.png` (360)
- `wen324-product-desktop.png` (qty 2, MAX 5 + custom-offer hintbox)
- `wen324-cart-desktop.png` (two lines, steppers, summary box)
- `wen324-checkout-company-desktop.png` (company mode, coupon APPLIED,
  discount row, gross keycap)
- `wen324-checkout-validation-error-desktop.png` (company without tax
  number → field-level role=alert)
- `wen324-confirmation-desktop.png` (REF-2026-0001 certificate)

## 9. Limitations / follow-ups

- Drizzle order persistence deferred (§4) — orders are process-local.
- `orders.place` records the payment authorization id but cannot VERIFY it
  against the PaymentPort (the port has no lookup op and stays untouched) —
  a client could pass an arbitrary string. Acceptable at the teaching
  boundary; a real system would verify or capture server-side.
- Checkout form state (contact/billing/coupon) resets if the buyer
  navigates back to the cart — wizard memory beyond the cart itself was not
  in scope.
- Coupon totals during an applied-unknown-coupon error: the summary box
  intentionally goes "Totals unavailable — fix the coupon below." and the
  pay button disables until the coupon is removed.
