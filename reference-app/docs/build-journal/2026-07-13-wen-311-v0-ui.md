# Build journal — WEN-311 v0 visual contract

**Date:** 2026-07-13

**Issue:** `WEN-311`

**Base:** `main@161d0f4c42d3a6896f902821f318ef1e7f412fc1`

**Branch:** `ai/wen-311-v0-ui-contract`

**Scope:** visual specification, v0 alternatives, selected presentation-layer
integration and evidence; no domain/schema/Auth/workflow behavior change

This record is written while the package runs. Linear owns current work state;
[`../../PLAN.md`](../../PLAN.md) owns the durable project plan.

## 1. Acceptance restatement before work

The package is complete only after a versioned design contract, three distinct v0
directions, a human design decision, bounded UI integration, desktop/mobile and state
evidence, consolidated application gates, fresh RUG review, verified bounce-back,
and a human merge decision. A generated mockup or a green local build alone is not
completion.

## 2. Setup and lease

The human authorized the complete reference-app programme. Four large issues were
created as a dependency chain; only WEN-311 entered In Progress.

```powershell
git pull --ff-only
git worktree add C:\Zulu\git_wt\wshp-ai-dev-2026\wen-311 `
  -b ai/wen-311-v0-ui-contract main
git -C C:\Zulu\git_wt\wshp-ai-dev-2026\wen-311 rev-parse HEAD
```

Observed base: `161d0f4c42d3a6896f902821f318ef1e7f412fc1`. The active
reference-app lease was posted before edits.

## 3. Current-truth inspection

The builder read the canonical plan, root/reference rules, parallel-work contract,
engineering standard, WEN-311 spec, current route/UI source, shadcn configuration,
and current official v0 Git Import/Design Mode guidance.

Observed UI truth:

- neutral default shadcn tokens and Geist already exist;
- three routes and the complete interaction logic already exist;
- health, CRUD, Auth, price, fake payment and registration states are implemented;
- the shell, visual hierarchy, mobile table strategy, journey progress and success
  presentation are not designed as one system.

## 4. Vercel secret-isolation gate

The worktree was linked locally to the existing project only for a read-only CLI
inventory. `.vercel/` remains ignored.

```powershell
vercel link --yes --project wshp-ai-dev-2026 --scope wenova-projects
vercel env ls
vercel integration list
```

Sanitized observation:

- encrypted `DATABASE_URL` and `DATABASE_URL_UNPOOLED` exist in Development and
  Production;
- the same names exist in several branch-scoped Preview environments;
- `NEON_AUTH_*` names were not present;
- no integration resources were returned by the CLI;
- no secret value was read, pulled, printed or stored.

Disposition: v0 must not connect to the production-linked Vercel project. Default to
Git Import without a project connection. If v0 requires a project, stop at a human
gate before creating a dedicated secret-free design project.

## 5. Browser and v0 capability gate

The approved browser surfaces were checked before attempting v0:

- Google Chrome is installed but was not running;
- the ChatGPT Chrome Extension was not installed/enabled in the available profiles;
- the native messaging host was present and valid;
- the in-app browser was unavailable in this session;
- `V0_API_KEY` and a local v0 MCP configuration were absent.

No unsupported browser automation or credential workaround was used. The local app
was started on port 3110 with the guarded in-memory E2E seam, ready for baseline
capture when an approved browser becomes available.

## 6. Pre-generation artifact

[`../design/v0-design-brief.md`](../design/v0-design-brief.md) now defines:

- product story, users and route purpose;
- complete state, responsive and accessibility contracts;
- allowed/protected files and behaviors;
- the Vercel secret-isolation decision;
- Precision Workshop Console, Editorial Field Guide and Mission Passport directions;
- one common bounded v0 prompt plus direction-specific input;
- weighted human selection rubric and required evidence.

This contract is committed before any generated code is accepted.

## 7. Human action required before generation

Install and enable the
[ChatGPT Chrome Extension](https://chromewebstore.google.com/detail/codex/hehggadaopoacecdllhhajmbjkdcmajg)
in the intended Chrome profile, start Chrome, sign in to v0 in that profile, and tell
the agent when it is ready. The agent will then reconnect, capture baseline images,
import without the production-linked Vercel project, and generate the three branches.

Do not connect v0 to `wshp-ai-dev-2026` and do not paste any environment value.

## 8. Validation, generation, review and integration log

Pending the browser/v0 human gate. This section will record exact prompts, sanitized
version identifiers, scorecard, integrated diff, commands, results, reviewer finding
dispositions, screenshots and residual risks before WEN-311 can close.
