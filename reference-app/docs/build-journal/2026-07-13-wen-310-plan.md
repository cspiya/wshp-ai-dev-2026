# Build journal — WEN-310 canonical plan and baseline

**Date:** 2026-07-13

**Issue:** `WEN-310`

**Base:** `main@7c213d50d63ea2af09531b0581a418c636204474`

**Branch:** `ai/wen-310-reference-app-plan`

**Scope:** planning, current-truth reconciliation, and journal only; no application behavior change

This is an immutable build record, not the current project status. Current state and
ownership stay in Linear; durable project decisions stay in [`../../PLAN.md`](../../PLAN.md).

## 1. Intended outcome

Create a repository-owned plan from which a session with no access to the originating
conversation can understand:

- what the reference app is and why it exists;
- what is already built and what is only partially proven;
- how v0 participates in visual design without owning architecture;
- how work continues in a few large packages;
- which human/agent gates and evidence make the result complete;
- how the final app will be replayed from a clean environment.

## 2. Preconditions and human decisions

1. The human decided that key project information must be versioned in Markdown, not
   stored only in Linear.
2. A separate Linear project, `wshp-ai-dev-reference-app-2026`, was approved under the
   existing workspace.
3. The human explicitly authorized moving WEN-310 to Todo/In Progress, producing the
   plan, and merging the reviewed result to `main`.
4. Existing GitHub, Linear, Vercel, and Neon accounts/workspaces are reused; isolation
   happens at project, branch, worktree, environment, and preview-database level.

## 3. Work setup — exact sequence

The coordinator performed these steps from the clean coordinator checkout:

```powershell
git status --short --branch
git pull --ff-only
New-Item -ItemType Directory -Force C:\Zulu\git_wt\wshp-ai-dev-2026 | Out-Null
git worktree add C:\Zulu\git_wt\wshp-ai-dev-2026\wen-310 `
  -b ai/wen-310-reference-app-plan main
git -C C:\Zulu\git_wt\wshp-ai-dev-2026\wen-310 rev-parse HEAD
git worktree list
```

Observed base: `7c213d50d63ea2af09531b0581a418c636204474`. The active
reference-app lease, path, branch, scope, timestamp, and exit contract were recorded
on WEN-310 before repository edits.

## 4. Current-truth audit — exact sequence

### 4.1 Rules and canonical standards

Read completely before editing:

- root `AGENTS.md`;
- `PARALLEL-WORK.md`;
- `reference-app/AGENTS.md`;
- `toolkit/standards/engineering-standards.md`;
- the Linear and v0 skill instructions active in the session.

### 4.2 Repository inventory

`rg` was attempted first as required by the operating rules but was unavailable in
the PowerShell environment. The audit fell back to tracked-file and native
PowerShell queries:

```powershell
git ls-files reference-app
git ls-files materials/epitesi-naplo
git ls-files reference-app/src/app
Get-ChildItem reference-app/src/modules -Directory
$markdownFiles = @(
  'materials/big-picture.md',
  'materials/fogalomtar.md',
  'reference-app/README.md',
  'reference-app/SETUP-STATUS.md',
  'toolkit/README.md'
)
$markdownFiles += Get-ChildItem reference-app/docs,toolkit -Recurse -File -Filter *.md |
  ForEach-Object FullName
Select-String -Path $markdownFiles -Pattern '^(#|##|###) '
git log --oneline -20 -- reference-app
```

### 4.3 Behavior and delivery evidence inspected

The audit read the actual artifacts rather than relying on earlier status prose:

- `reference-app/README.md` and `SETUP-STATUS.md`;
- ADR 0001 and the 2026-07-12 technical-verification record;
- application routes, shop UI, Auth client/server/route, tRPC request context, and
  protected procedures;
- Playwright specs and package scripts;
- CI, preview-E2E, and material/publication workflows;
- workshop big picture, toolkit setup, memory, hook, RUG, and C0–C7 descriptions;
- Day 4 journal structure and the journal index.

Representative commands:

```powershell
Get-Content -Raw -Encoding utf8 reference-app/README.md
Get-Content -Raw -Encoding utf8 reference-app/SETUP-STATUS.md
Get-Content -Raw -Encoding utf8 reference-app/docs/verification/2026-07-12-final-technical-verification.md
Get-Content -Raw -Encoding utf8 reference-app/src/platform/api/root.ts
Get-Content -Raw -Encoding utf8 reference-app/e2e/shop.spec.ts
Get-ChildItem .github/workflows -File
git grep -n -E 'getAuth|isAuthConfigured|userId|session|UNAUTHORIZED' -- reference-app/src reference-app/e2e
```

### 4.4 External fact verification

Because v0 changes quickly, the plan used the current official documentation rather
than remembered behavior:

- [Git Import](https://v0.app/docs/git-import): existing repositories, monorepo root
  selection, existing Vercel project connection, dedicated branch, PR workflow;
- [Design Mode](https://v0.app/docs/design-mode): live visual edits, pending changes,
  diffable/revertible applied versions, Tailwind awareness, and mobile limitation.

## 5. Findings and resulting decisions

| Finding | Evidence | Plan consequence |
|---|---|---|
| The reference app is not empty or merely scaffolded | real modules, routes, migrations, tests and previous deployment records | complete the existing app; do not restart it |
| The full invented shop journey exists | `/shop`, four feature modules, router tests and local Playwright | preserve behavior while redesigning presentation |
| Thin Neon Auth code is already merged | package, server wrapper, proxy route, session context, protected write tests, auth panel | correct stale setup claims; separate code-complete from live-proof-complete |
| Final live Auth proof is absent | unchecked env/provisioning items and no accepted post-auth Preview E2E record | make authenticated Preview browser/API/data proof a blocking gate |
| Visual design is functional but unfinished | basic shadcn layout with no accepted app-specific guideline/state matrix | add an explicit v0 alternatives + human design gate phase |
| Existing docs are useful but not a zero-to-production replay | README/setup/ADR/evidence are distributed and historically scoped | create scripts plus one ordered clean-replay runbook |
| Parallel material work is active | WEN-129 owns material/toolkit surfaces | reference lane publishes only an accepted SHA/evidence packet to Claude |

## 6. Artifacts produced

- `reference-app/PLAN.md` — canonical mission, baseline, decisions, phases, v0
  contract, C0–C7 mapping, gates, evidence, risks, responsibilities, and definition
  of done;
- `reference-app/SETUP-STATUS.md` — corrected distinction between merged Auth code
  and missing live integration proof;
- `reference-app/README.md` — reconciled the summary with the actual merged Auth code;
- this detailed reference-app build record;
- `materials/epitesi-naplo/day-5.md` — cross-workshop synthesis in the required
  diagram/human-loop/agent-loop/collapsed-case format;
- the build-journal index entry for Day 5.

## 7. Validation and fresh review

### 7.1 Consolidated maker checks

Run from the repository root after staging the complete documentation batch:

```powershell
git diff --cached --check
node toolkit/hooks/check-placeholders.mjs
node toolkit/hooks/check-notebooks.mjs
node toolkit/hooks/check-links.mjs
node toolkit/hooks/check-public-content.mjs
npx npm@10 ci --prefix reference-app
npm --prefix reference-app run typecheck
npm --prefix reference-app run lint
npm --prefix reference-app run test
```

Results before fresh review:

| Gate | Result |
|---|---|
| staged whitespace | PASS after removing Markdown hard-break whitespace |
| placeholder scan | PASS, 86 files |
| notebook structure | PASS, 8 files |
| relative links | PASS, 117 files |
| public-content guard | PASS, 117 files |
| npm 10 clean install | PASS, 821 packages installed |
| typecheck | PASS |
| lint | PASS |
| unit/contract tests | PASS, 65 passed; 11 DB-dependent tests skipped without `TEST_DATABASE_URL` |

The install reported an engine warning because the machine had Node 22.12 while a
transitive lint dependency requests 22.13 or newer. It also reported 10 existing npm
audit findings (9 moderate, 1 critical). The package/lockfile was not changed in this
documentation issue. Both are explicit residual risks for the implementation
packages; the plan now requires Node 22.13 or newer.

### 7.2 Independent fresh-context review and disposition

The reviewer received the staged diff, issue acceptance criteria, repository rules,
and canonical engineering standard — not the builder's reasoning. It verified the
claims from source, tests, workflows, current official v0 documentation, and the
canonical C0–C7 material.

| Finding | Severity | Verification and disposition |
|---|---|---|
| Connecting v0 to the production-linked Vercel project exposes its env variables to v0 | High | ACCEPTED; official Git Import docs and current setup confirm the risk. Added a human pre-import inventory and a disconnected/dedicated secret-free design project rule. |
| C4/C5 mapping was reversed | High | ACCEPTED; corrected to canonical C4 RUG and C5 mechanical positive/negative gates. |
| Assumptions/open decisions were implicit and WEN-310 merge status contradicted itself | High | ACCEPTED; added explicit registers and recorded the actual 2026-07-13 merge approval. |
| Public artifacts contained access-controlled Linear URLs | High | ACCEPTED; removed project/issue hyperlinks and kept plain public-safe identifiers. |
| Journal contained a non-runnable placeholder and no result log | Medium | ACCEPTED; replaced it with executable PowerShell and completed this section. |
| README repeated the stale “Auth package missing” claim | Medium | ACCEPTED; reconciled it with package, route, session, protection and remaining live proof. |

The reviewer also ran a production build successfully. No application behavior was
changed.

Post-fix checks and bounce-back result:

| Re-check | Result |
|---|---|
| `git diff --cached --check` | PASS |
| placeholder scan | PASS, 86 files |
| notebook structure | PASS, 8 files |
| relative links | PASS, 117 files |
| public-content guard | PASS, 117 files |
| replacement PowerShell inventory command | PASS; no non-executable placeholder remains |
| fresh bounce-back review | five findings VERIFIED resolved; the journal result-recording gap was the only remaining medium finding; no new high/blocker |

The journal gap was then fixed with this result table. The affected documentation
gates passed once more with the same counts above, and the final targeted fresh
reviewer returned `VERIFIED`: no blocking/high finding or material completeness gap
remained.

Merge commit, push, and worktree/lease release evidence are recorded in WEN-310 after
they occur; a pre-merge commit cannot truthfully self-record its future merge SHA.

## 8. Replay of this planning package

A fresh session can reproduce the reasoning without this chat by:

1. reading WEN-310 and the active repository rules;
2. checking out the base commit above in an isolated worktree;
3. running the inventory and evidence queries in section 4;
4. comparing each row of section 5 with the cited files;
5. reading `PLAN.md` and verifying that every finding has a corresponding boundary,
   phase, gate, or evidence requirement;
6. running the consolidated validation commands recorded in section 7.1;
7. reviewing the diff against the WEN-310 acceptance criteria and canonical
   engineering standard.
