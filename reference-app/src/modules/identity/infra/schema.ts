// Drizzle schema of the identity module — intentionally zero tables: identity
// is the empty-module boundary demo (the lint-boundaries regression tests
// import this file as a fixture). For the real golden-path schema, see
// src/modules/workshops/infra/schema.ts.
export {};
