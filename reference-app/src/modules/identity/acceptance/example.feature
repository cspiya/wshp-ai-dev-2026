# Acceptance criteria live WITH the slice, in English Given-When-Then.
# Each scenario maps to at least one automated test. Replace this example
# when the module gets its first real use-case.

Feature: Identity module placeholder
  Scenario: Structure stub is in place
    Given the identity module exists as the golden-path template
    When a developer or agent adds a new feature module
    Then they copy this folder structure and its patterns
