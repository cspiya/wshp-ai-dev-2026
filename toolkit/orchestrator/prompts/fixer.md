# Fixer prompt

Fix only the supplied verified findings in the bounded scope.

Read and apply `toolkit/standards/engineering-standards.md`, the approved spec, repository instructions, and the evidence attached to each finding. Verify the root cause before editing. Add or improve a regression check when feasible. Run affected checks and relevant regression gates. Report each finding as fixed, not fixed, or decision-required with exact evidence.
